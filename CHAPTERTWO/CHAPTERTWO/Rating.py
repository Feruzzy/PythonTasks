rating = input('Enter an integer rating between 1 and 10')

#Results Enter an integer rating between 1 and 10 - it prints the above but does not have : and no other operation after the user gives an input
