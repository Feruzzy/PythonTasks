"""
Internal iteration (like map, filter, or list comprehensions) is preferable because 
the library/language handles the "how" (looping logic), allowing for easier 
parallelization and more concise code compared to external 'for' loops.
"""

