"""
Population Variance (pvariance) divides by N (the total count).
Sample Variance (variance) divides by N - 1 (Bessel's correction).
The reason is that using N - 1 provides an unbiased estimate of the population 
variance when working with a smaller subset (sample) of data.
"""
